name: Monthly SBOM Refresh

# DISABLED: this called a reusable workflow in <REDACTED>/phenotype-tooling,
# which no longer resolves, so every run failed at startup without a job ever
# starting. Same treatment as vitepress-pages.yml and the promote job in
# release.yml. Restore the `uses:` call below only once that repository is
# reachable.

on:
  schedule:
    - cron: "0 0 1 * *" # 1st of each month
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: disabled (upstream reusable workflow is gone)
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "SBOM refresh is disabled; see the note at the top of this file."
