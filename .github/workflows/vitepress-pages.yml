name: VitePress Pages (disabled)
# DISABLED: depends on missing repo KooshaPari/heliosApp (404).
# The canonical Pages workflow is pages.yml. This file is preserved for
# provenance and only runs via manual workflow_dispatch so the dependency
# is visible but does not block PRs/main.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-notice:
    name: Workflow disabled
    runs-on: ubuntu-24.04
    timeout-minutes: 1
    steps:
      - name: Report disabled integration
        run: |
          echo "::notice title=VitePress Pages disabled::The heliosApp reusable dependency is no longer resolvable. Use pages.yml instead."
