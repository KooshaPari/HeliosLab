name: Tag Automation

# DISABLED: this called a reusable workflow in <REDACTED>/phenoShared, which no
# longer resolves, so every run failed at startup without a job ever starting.
# Same treatment as vitepress-pages.yml and the promote job in release.yml.
# Restore the `uses:` call below only once that repository is reachable.

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: disabled (upstream reusable workflow is gone)
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Tag automation is disabled; see the note at the top of this file."
