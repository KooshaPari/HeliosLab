name: Alert sync issues

on:
  schedule:
    - cron: '17 * * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sync:
    uses: KooshaPari/phenoShared/.github/workflows/reusable/alert-sync-issues.yml@5ed0e3bb2279610656755d3adb5403fba94a2bd2
    with:
      auto-label: auto-alert-sync
