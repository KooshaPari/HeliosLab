[package]
name = "pheno-cli"
version.workspace = true
edition.workspace = true

[[bin]]
name = "phenoctl"
path = "src/main.rs"

[dependencies]
pheno-core = { path = "../pheno-core" }
pheno-db = { path = "../pheno-db" }
pheno-crypto = { path = "../pheno-crypto" }
clap = { version = "4", features = ["derive"] }
chrono = "0.4"
ratatui = { version = "0.30", default-features = false, features = ["crossterm", "underline-color", "all-widgets", "macros", "layout-cache"] }
crossterm = "0.29"
hex = "0.4"
rpassword = "7"
