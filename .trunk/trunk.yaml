# =============================================================================
# Trunk.io — Plugin versions for linting/formatting tools
# =============================================================================
# Auto-generated. Run `trunk upgrade` to update.
# https://docs.trunk.io/check/reference
# =============================================================================
version: 0.1

plugins:
  sources:
    # Trunk now requires `uri` on plugin sources. Without it the launcher reports
    # "plugin must set `uri` and `ref` but `uri` was not set" as a high-severity
    # trunk/config-error, and the Trunk Check job fails before linting anything.
    - id: trunk
      uri: https://github.com/trunk-io/plugins
      ref: v1.2.2
    # The community source needs the same uri. CI confirmed this rather than my
    # guessing: after fixing the trunk source the error simply moved from
    # "[trunk] ... 11:7" to "[community] ... 17:7", same trunk/config-error.
    - id: community
      uri: https://github.com/trunk-io/plugins
      ref: main

cli:
  version: 1.22.12
